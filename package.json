{
  "name": "multi-server",
  "version": "1.0.0",
  "description": "Run multiple node webservers through different domains",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Matheson",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "http": "0.0.0",
    "http-proxy": "^1.16.2"
  }
}
